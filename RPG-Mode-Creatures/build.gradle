version = getSubprojectVersion(project)
archivesBaseName = "rpgmodecreatures"

moduleDependencies(project, [
        'RPG-Mode-Core',
        'RPG-Mode-Effects'
])

dependencies {
    include modImplementation('software.bernie.geckolib:geckolib-fabric-1.19.3:4.0.6')
}